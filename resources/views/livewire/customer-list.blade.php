<div><form wire:submit="{{ $editingCustomerId === null ? 'save' : 'update' }}"><input wire:model="name" placeholder="Customer name"><input wire:model="code" placeholder="Code"><input wire:model="email" type="email" placeholder="Email"><button type="submit">{{ $editingCustomerId === null ? 'Add customer' : 'Update customer' }}</button>@if ($editingCustomerId !== null)<button type="button" wire:click="cancelEdit">Cancel</button>@endif</form>@error('name')<p>{{ $message }}</p>@enderror<ul>@forelse($customers as $customer)<li>{{ $customer->name }} ({{ $customer->code }}) <button type="button" wire:click="edit({{ $customer->id }})">Edit</button> <button type="button" wire:click="delete({{ $customer->id }})">Delete</button></li>@empty<li>No customers yet.</li>@endforelse</ul></div>
