<div><form wire:submit="{{ $editingSiteId === null ? 'save' : 'update' }}"><select wire:model="customer_id"><option value="">Customer</option>@foreach ($customers as $customer)<option value="{{ $customer->id }}">{{ $customer->name }}</option>@endforeach</select><input wire:model="name" placeholder="Site name"><input wire:model="code" placeholder="Code"><button type="submit">{{ $editingSiteId === null ? 'Add site' : 'Update site' }}</button>@if ($editingSiteId !== null)<button type="button" wire:click="cancelEdit">Cancel</button>@endif</form>@error('customer_id')<p>{{ $message }}</p>@enderror<ul>@forelse($sites as $site)<li>{{ $site->name }} ({{ $site->code }}) — {{ $site->customer?->name }} <button type="button" wire:click="edit({{ $site->id }})">Edit</button> <button type="button" wire:click="delete({{ $site->id }})">Delete</button></li>@empty<li>No sites yet.</li>@endforelse</ul></div>
